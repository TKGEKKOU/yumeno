# YUMENO 设计基线

## 视觉世界：近黑场域 · 发丝线

启动页是 YUMENO 的第一块门面：近黑场域（`#0a0a0b`）、1px 发丝进度线、
等宽百分比读数、一行当前进度文案。只保留必要信息——无字标、无装饰、
无分组标签。一次只讲一件事：一条线从无到有扫过全屏，数字前进，
一行字说明此刻正在做什么。明暗选择来自使用场景：桌面应用冷启动的瞬间，
安静、聚焦、无卡片无边框。

### 令牌（启动页）

- 背景 `#0a0a0b`；前景 `#e8e8ea`；次级 `#8f8f96`；弱化 `#56565c`
- 进度轨道 `#212124`；进度线 `#e4e4e6`；失败 `#e58a8f`
- 动效 `360ms cubic-bezier(.2,.7,.2,1)`；进度只动 `transform: scaleX`
- 等宽读数：`Cascadia Mono / Consolas`，tabular-nums；字号档位 10 / 13 / 20px

### 行为规则

- 打开即自动启动（不再需要点击"启动"）；依赖服务不单独罗列，
   细节体现在当前阶段文案里（如"正在拉起 Milvus 容器…"）
- 全屏单音频：任意音频开始播放时，其余全部暂停（聊天流式、预览、试听共用）
- 首次用户交互解锁 AudioContext（自动播放策略），Live2D 口型依赖该恢复

### 明确不做的

- 不出现字标、卡片、渐变字、表情符号图标；不做任何装饰性元素

---

# 全局视觉：明日方舟官网风（2026-08-06 用户指令）

应用整体从"中性浅灰"转向《明日方舟》官网的视觉语言：黑白灰基底 +
青色强调（官网主站强调色 `#18D1FF`，客服中心强调色 `#00AFEC`）、
直角硬边、技术标注感。启动页的"近黑场域"作为同一世界的深色段落保留。

## 令牌（全局）

- 背景 `#f5f5f5`（客服中心式干净浅灰）；表面 `#ffffff`；墨线 `#18171a`；次级 `#6f6f6f`；弱化 `#9c9c9c`
- 强调色 = 青 `#00AFEC`（客服中心原色；深色主题用官网主站 `#18D1FF`）；危险红 `#b3261e` 仅用于删除/失败等语义动作
- 线条 `#e7e7e8` / 深 `#d1d1d1`；硬阴影 `4px 4px 0 rgba(20,20,20,.9)`（关键卡片/按钮）
- 圆角：直角为主（0–2px）；按钮、标签用 1px 描边 + 间距字距（letter-spacing）
- 技术标注：等宽字体（Cascadia Mono/Consolas）小号大写，`//` 前缀（如 `// VOICE STUDIO`）
- 深色主题：近黑 `#0d0d0f`、墨白 `#e8e8ea`、强调 `#18D1FF`
- 侧边栏：黑白（浅色主题黑底白字，深色主题近黑底白字），悬停青色

## 组件规则

- 按钮：黑底白字或白底黑边，直角，悬停青色边/青色字；禁用透明度保持现有约定
- 面板：白底 + 1px 墨线；标题区带 `//` 标注；不使用圆角阴影卡片
- 导航：直角描边，悬停青色边/青色字（不采用侧边色条）
- 状态：完成/运行中/失败沿用现有语义色，但标签改为直角描边小标签
- 声音工坊：有向流程为纵向节点链（箭头连接），节点可投放文件（中途插入）

## 明确不做

- 不加装饰性图案、渐变、圆角气泡；不加虚构图片素材
- 不引入网络字体（离线桌面应用），用系统字体 + 字距/字重模拟气质
